#include <iostream>
#include <vector>
#include <algorithm>
#define all(a) (a).begin(), (a).end()
using ll = long long;
using namespace std;

int main() {
    int n, m; 
    cin >> n >> m;
    cout << (n + m) * (n + m);
}