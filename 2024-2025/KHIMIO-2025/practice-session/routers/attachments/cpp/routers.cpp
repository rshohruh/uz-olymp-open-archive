#include "routers.h"
using namespace std;

std::vector<int> find_routers(int l, int n, int q) {
    int idx = use_detector(0);
    std::vector<int> ans;
    for (int i = 0; i < n; i++) {
		ans.push_back(0);
	}
	return ans;
}
